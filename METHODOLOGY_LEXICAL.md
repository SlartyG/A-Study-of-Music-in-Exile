# Методология: лексический анализ тональности

Метод 1 из исследования «Музыка после отъезда».  
Скрипт: `analysis/lexical_sentiment.py`  
Результаты: `analysis/output/`

---

## 1. Цель

Численно сравнить тональность текстов российских музыкантов-эмигрантов в двух периодах — до и после релокации, — используя словарный (лексический) подход без нейросетевых компонентов.

---

## 2. Источник данных

| Файл | Содержание |
|---|---|
| `data/songs.jsonl` | Тексты треков, собранные через Genius API |
| `data/sample_musicians.csv` | Профили 50 артистов с датами релокации и флагом `genius_ok` |
| `data/rusentilex_2017.txt` | Словарь тональности RuSentiLex 2017 |

В анализ включаются только артисты с `genius_ok = TRUE` (24 из 50). Остальные исключены из-за недостаточного числа треков (< 10 до или < 10 после) или отсутствия на Genius.

---

## 3. Словарь тональности: RuSentiLex 2017

**RuSentiLex** (Loukachevitch & Levchik, 2016) — стандартный словарь для русскоязычного NLP. Содержит ~16 000 записей с разметкой:
- `positive` → +1
- `negative` → −1
- `neutral` → 0
- `positive/negative` → пропускается (контекстно-зависимые слова)

В словарь добавляются и словарные формы, и леммы каждой записи, что увеличивает покрытие без необходимости полного морфологического анализа. Итого в рабочем словаре ≈ 10 800 ключей (однословных).

**Ограничение.** Словарь не учитывает контекст: отрицания, иронию и переносные значения. Это принципиальное ограничение метода 1, частично компенсируемое методом 3 (ruBERT).

---

## 4. Предобработка текстов

1. **Удаление служебных элементов Genius:** строки вида `N Contributors … Lyrics` и заголовки секций `[Куплет 1]`, `[Припев]`, `[Verse]` и т. п.
2. **Токенизация:** извлекаются только кириллические последовательности символов (регулярное выражение `[а-яёА-ЯЁ]+`), что отсеивает транслит, числа, знаки препинания и иноязычные вставки.
3. **Нормализация:** все токены приводятся к нижнему регистру и сопоставляются со словарём.

Треки с менее чем 20 кириллическими словами исключаются из анализа.

---

## 5. Метрики тональности

Для каждого трека вычисляются следующие показатели:

| Метрика | Формула | Интерпретация |
|---|---|---|
| `sentiment_ratio` | (pos − neg) / total_words | **Основная.** Относительный перевес позитивных слов во всём тексте |
| `polarity` | (pos − neg) / (pos + neg) | Полярность среди тональных слов |
| `pos_ratio` | pos / total_words | Доля позитивных слов |
| `neg_ratio` | neg / total_words | Доля негативных слов |
| `tonal_density` | (pos + neg) / total_words | Насыщенность тональными словами |

`sentiment_ratio` ∈ [−1, 1]; значение 0 означает нейтральный или сбалансированный текст.

---

## 6. Разметка периодов

Границей периодов служит поле `reloc_date` из `sample_musicians.csv`.  
- **До** — треки, выпущенные *строго до* даты релокации (`period = "before"`).  
- **После** — треки, выпущенные *начиная с* даты релокации (`period = "after"`).

Логика разметки подробно описана в `README.md` (раздел «Разметка периодов»).

---

## 7. Сбалансированная выборка

Все сравнения «до / после» ведутся **только на сбалансированной выборке**. Для каждого артиста:

```
N = min(число треков «до», число треков «после»)
before_sample = N самых новых треков из периода «до» (по release_date)
after_sample  = N самых новых треков из периода «после»
```

**Обоснование.** Исключает перекос, при котором период «до» охватывает 10–15 лет карьеры, а «после» — 2–4 года. Без балансировки агрегированные средние отражают не сдвиг после эмиграции, а разный объём и временной охват периодов.

**Ограничение.** Ранние треки карьеры не попадают в выборку. Сравнение фиксирует изменение в «окне» непосредственно до и после отъезда, а не эволюцию за всю карьеру.

Артисты, у которых в одном из периодов нет треков (например, Скриптонит — только «после»), исключаются из сбалансированного корпуса.

---

## 8. Статистические тесты

| Тест | Зачем |
|---|---|
| **Mann–Whitney U** | Непараметрический; не предполагает нормальности распределений тональности |
| **Cohen's d** | Размер эффекта: d < 0.2 — пренебрежимо малый, 0.2–0.5 — малый, 0.5–0.8 — средний, > 0.8 — большой |

Тесты проводятся:
1. На всей сбалансированной совокупности (aggregated).
2. Отдельно для каждого артиста.

Уровень значимости: α = 0.05. Значимые результаты помечаются звёздочками (* p < 0.05, ** p < 0.01, *** p < 0.001).

---

## 9. Визуализации

| Файл | Описание |
|---|---|
| `1_violin_overall.png` | Скрипичная диаграмма: распределение тональности «до»/«после» |
| `2_artist_bars.png` | Горизонтальные бары: среднее «до»/«после» по каждому артисту |
| `3_scatter_before_after.png` | Scatter: каждый артист — точка; ось X = тональность «до», ось Y = «после» |
| `4_kde_distribution.png` | Кривые плотности: форма распределения тональности в двух периодах |
| `5_delta.png` | Дельта-диаграмма: изменение тональности (после − до) по артистам |
| `6_timeline.png` | Временной ряд по году выпуска (см. примечание ниже) |
| `7_heatmap.png` | Тепловая карта: индекс тональности «до»/«после» по артистам |

**Примечание к `6_timeline.png`.** На этом графике периоды задаются **календарно**, а не по дате релокации: год выпуска < 2022 → линия «до»; ≥ 2022 → линия «после». В точке 2022 синяя линия показывает итог всего пред-2022 корпуса, оранжевая — среднее за 2022 год. Это визуализация динамики по годам, а не основной тест гипотезы.

Логика timeline реализована в `analysis/timeline.py`.

---

## 10. Результаты

*Дата прогона: июнь 2026. Корпус: сбалансированная выборка.*

| Показатель | Значение |
|---|---|
| Треков в выборке | 1 168 (584 до / 584 после) |
| Артистов | 21 |
| Среднее «до» | −0.0070 |
| Среднее «после» | −0.0049 |
| Cohen's d | +0.064 (пренебрежимо) |
| p-value | 0.378 (n.s.) |

**Интерпретация.** Оба периода слабо-негативны (жанровая норма для рэп/рок корпуса). На сбалансированной выборке **агрегированного сдвига тональности нет**.

### Артисты со значимым сдвигом (p < 0.05)

| Артист | Δ | p | d |
|---|---|---|---|
| Максим Покровский | −0.0079 | 0.002 | −0.523 |
| Арсений Морозов | +0.0206 | 0.008 | +0.334 |
| Борис Гребенщиков | −0.0083 | 0.041 | −0.354 |
| Ushko | −0.0018 | 0.013 | −0.064 |

---

## 11. Выходные файлы

| Файл | Содержание |
|---|---|
| `sentiment_scores.csv` | Тональность каждого трека (только сбалансированная выборка) |
| `per_artist.csv` | Агрегированные результаты по артистам |

---

## 12. Ограничения метода

1. **Контекст не учитывается.** Отрицание («не рад»), ирония, метафора не обрабатываются.
2. **Мат и разговорная лексика.** Значительная часть тропов рэп-текстов отсутствует в словаре.
3. **Мультиязычные вставки.** Треки с украинским или английским текстом теряют часть токенов при фильтрации кириллицы.
4. **Короткое окно «после».** У ряда артистов период «после» охватывает 2–4 года; балансировка сравнивает равные N треков, но не равные календарные интервалы.

---

## 13. Запуск

```bash
python3 analysis/lexical_sentiment.py
```

Результаты: CSV-файлы и PNG-графики в `analysis/output/`.  
RuSentiLex скачивается автоматически при первом запуске.

**Зависимости:**
```
pandas numpy matplotlib seaborn scipy
```
